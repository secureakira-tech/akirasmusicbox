# Local BMAD config (personal overrides, not shared)
_bmad/config.user.toml
_bmad/custom/config.user.toml
_bmad/custom/*.user.toml
_bmad/.claude/

# OS
.DS_Store
Thumbs.db

# GPG — private key material must never be committed
*.key
*_private.asc
*_secret.asc
revoke-*.asc
secret-*.asc
